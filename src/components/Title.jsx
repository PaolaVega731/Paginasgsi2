import React from "react";

const SectionTitle = ({ text }) => {
  return (
    <h2 className='text-[54px] font-bold leading-[77px] drop-shadow-2xl textTitle'>
      {text}
    </h2>
  );
};

export default SectionTitle;
